import { HeroTemplate, OurRoutesTemplate } from "@/templates";

const Homepage = () => {
  return (
    <>
      <HeroTemplate />
      <OurRoutesTemplate />
    </>
  );
};

export default Homepage;
