import { HeroTemplate, OurRoutesTemplate, ParcelsTemplate } from "@/templates";

const Homepage = () => {
  return (
    <>
      <HeroTemplate />
      <OurRoutesTemplate />
      <ParcelsTemplate />
    </>
  );
};

export default Homepage;
